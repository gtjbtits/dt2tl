spring:
  datasource:
    url: jdbc:h2:./g2t
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
logging:
  level:
    root: info
    com.jbtits: ${log_jbtits:info}
botapi:
  token: ${botapi_token}
  username: ${botapi_username}
pr:
  prefix: ${pr_prefix}
